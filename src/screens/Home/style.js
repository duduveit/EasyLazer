import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
    container: {
      flex: 1,
      padding: 20,
      backgroundColor: '#fff',
    },
    loc: {
      left: 3,
      flexDirection: 'row',
      alignItems: 'center',
    },
    containerCards:{
      alignContent:'center',
      alignItems:'center',
    },
});

export default style